name: Hello Actions

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      # No third-party actions — proves runners work under strict org policy.
      # After Actions settings allow `actions/checkout`, you can add checkout back.
      - name: Hello
        run: echo "Actions OK"
